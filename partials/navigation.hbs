<ul class="space-y-4">
    {{#foreach navigation}}
        <li>
            <a href="{{url}}" class="text-anthracite dark:text-darkText hover:text-orangeValencia dark:hover:text-orangeValencia transition-colors duration-200">
                {{label}}
            </a>
        </li>
    {{/foreach}}
</ul>
