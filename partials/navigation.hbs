<ul class="space-y-4">
    {{#foreach navigation}}
        <li>
            <a href="{{url}}" class="text-anthracite hover-orangeValencia transition-colors duration-200">
                {{label}}
            </a>
        </li>
    {{/foreach}}
</ul>
