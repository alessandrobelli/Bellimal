<!DOCTYPE html>
<html lang="en" class="no-transition-on-load">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Fira+Mono&family=Atkinson+Hyperlegible&display=swap"
          rel="stylesheet">
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp,container-queries"></script>
    <link rel="stylesheet" href="{{asset "css/main.css"}}">
    <link rel="stylesheet" href="{{asset "css/animations.css"}}">
    <script>
        // Immediately apply dark mode if stored in localStorage (prevents flash)
        if (localStorage.getItem('darkMode') === 'true') {
            document.documentElement.classList.add('dark');
        } else if (localStorage.getItem('darkMode') === null && window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
            document.documentElement.classList.add('dark');
        }
    </script>
    <title>{{meta_title}}</title>
    {{ghost_head}}
</head>
<body class="bg-white dark:bg-dark text-anthracite dark:text-darkText transition-colors duration-300">
<div class="flex flex-col lg:flex-row min-h-screen">
    <!-- Navigation Section -->
    <div class="w-full lg:w-64 bg-white dark:bg-darkSecondary border-r dark:border-gray-800 p-6 lg:fixed lg:h-screen flex lg:flex-col items-center justify-center transition-colors duration-300">
        <nav class="flex flex-col items-center justify-center w-full">
            {{navigation}}
            <div class="mt-8">
                <button id="darkModeToggle" class="p-2 rounded-full focus:outline-none transition-colors duration-300">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 dark-icon hidden dark:block" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                    </svg>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 light-icon block dark:hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                    </svg>
                </button>
            </div>
        </nav>
    </div>

    <main class="w-full lg:ml-64 p-8 flex-grow overflow-auto">
        {{{body}}}
    </main>
    <script src="{{asset "js/darkmode.js"}}"></script>
    <script src="{{asset "js/animations.js"}}"></script>
    {{ghost_foot}}
</div>
</body>
</html>
